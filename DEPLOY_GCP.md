# OmniPsyche GCP Deployment

The current Google Cloud deployment guide lives at
[`docs/deployment-gcp.md`](docs/deployment-gcp.md).

OmniPsyche uses Gemini API key authentication server-side. Vertex AI
environment variables and service-account JSON credentials are not required
for AI generation.
